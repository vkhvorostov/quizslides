# 1 = SQLite (без PostgreSQL). 0 = PostgreSQL (нужна БД quizslides_bd в pgAdmin).
USE_SQLITE=1

# PostgreSQL (когда USE_SQLITE=0) — пароль как при входе в pgAdmin
DB_NAME=quizslides_bd
DB_USER=postgres
DB_PASSWORD=change_me
DB_HOST=localhost
DB_PORT=5432

SECRET_KEY=your-secret-key-here
DEBUG=True
